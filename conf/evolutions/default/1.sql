# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "EXPENSES" ("AMOUNT" DOUBLE NOT NULL,"DATE" DATE NOT NULL,"DESCRIPTION" VARCHAR NOT NULL,"ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY);
create table "USERS" ("EMAIL" VARCHAR NOT NULL,"PASSWORD" VARCHAR NOT NULL,"ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY);

# --- !Downs

drop table "USERS";
drop table "EXPENSES";

